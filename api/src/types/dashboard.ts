// Types related to dashboard layouts

// Example: Define the structure of the layout JSON if needed
// export interface DashboardLayoutItem {
//   i: string;
//   x: number;
//   y: number;
//   w: number;
//   h: number;
//   minW?: number;
//   minH?: number;
//   // Add other potential properties if necessary
// }

// export type DashboardLayout = DashboardLayoutItem[];

// We might not need specific types here if we just pass the JSONB directly
